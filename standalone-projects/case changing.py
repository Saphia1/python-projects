name=input("entername")
len(name)=length
for i in range (length):
    if (length[i]) == (length[i]).isupper:
        (name[i])=(length[i]).islower
    elif (length[i]) == (length[i]).islower:
        (length[i]) = (length[i]).isupper
    new=new+i
    print (new)
